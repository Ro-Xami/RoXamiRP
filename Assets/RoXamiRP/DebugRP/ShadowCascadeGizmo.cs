using UnityEngine;
using UnityEngine.Rendering;

public class ShadowCascadeGizmo : MonoBehaviour
{
    //void OnDrawGizmos()
    //{
    //    var spheres = Shadows.;

    //    Gizmos.color = Color.yellow;
    //    for (int i = 0; i < 4; i++)
    //    {
            
    //        Vector3 center = new Vector3(spheres[i].x, spheres[i].y, spheres[i].z);
    //        float radius = Mathf.Sqrt(spheres[i].w);
    //        Gizmos.DrawWireSphere(center, radius);

    //        Debug.Log(center);
    //        Debug.Log(radius);
    //    }
    //}
}
